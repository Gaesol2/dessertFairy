diff a/src/main/java/com/shop/dessertFairy/pay/service/PayServiceImpl.java b/src/main/java/com/shop/dessertFairy/pay/service/PayServiceImpl.java	(rejected hunks)
@@ -62,25 +62,25 @@
             e.printStackTrace();
         }
         
-		
+      
         return returnMap;
-		
-	}
-	
-	 public String getSHA256Hash(String input) throws NoSuchAlgorithmException {
-	        // SHA-256 해시 함수 생성
-	        MessageDigest md = MessageDigest.getInstance("SHA-256");
+      
+   }
+   
+    public String getSHA256Hash(String input) throws NoSuchAlgorithmException {
+           // SHA-256 해시 함수 생성
+           MessageDigest md = MessageDigest.getInstance("SHA-256");
 
-	        // 입력 문자열을 바이트 배열로 변환
-	        byte[] bytes = md.digest(input.getBytes(StandardCharsets.UTF_8));
+           // 입력 문자열을 바이트 배열로 변환
+           byte[] bytes = md.digest(input.getBytes(StandardCharsets.UTF_8));
 
-	        // 바이트 배열을 16진수 문자열로 변환
-	        StringBuilder hexStringBuilder = new StringBuilder();
-	        for (byte b : bytes) {
-	            String hex = String.format("%02x", b);
-	            hexStringBuilder.append(hex);
-	        }
+           // 바이트 배열을 16진수 문자열로 변환
+           StringBuilder hexStringBuilder = new StringBuilder();
+           for (byte b : bytes) {
+               String hex = String.format("%02x", b);
+               hexStringBuilder.append(hex);
+           }
 
-	        return hexStringBuilder.toString();
-	    }
-}
+           return hexStringBuilder.toString();
+       }
+}
\ No newline at end of file
